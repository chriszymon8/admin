assets/products.json
yung mga products dun na magbabago ng image yung mga malalabo


1. dapat unahin ito bago mag run ng file
command:
    cd FinalProject/backend
    npm install express
    npm install cors
    npm install ws
    node server.js

2. admin password saka user yung admin folder nandun yung file ng admin
    password:
    admin@gentry.com
    admin123


    const img = document.createElement("img");
    img.className = "w-16 h-16 object-contain";
    tdImg.appendChild(img);
    tr.appendChild(tdImg);